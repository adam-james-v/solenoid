(ns solenoid.core
  (:require [solenoid.controls :as controls]
            [solenoid.server :as server]))

;; TOOD: make an API here that will automatically spawn a server the first time you use letcontrols.
